import { useContext } from "react";
import LoginStatusContext from "../LoginStatusContext";

const UseLoginStatusContext = () => useContext(LoginStatusContext);

export default UseLoginStatusContext;
